# 防災アプリ機能移植用プロンプト

以下の指示を、対象プロジェクトを一度で完成状態に近づけるための実装依頼として使用してください。

---

あなたは既存の防災情報 Web アプリを改修するシニアエンジニアです。まずリポジトリの構成、認証、共通レイアウト、避難所登録処理、データ保存形式を調査してください。既存のフレームワーク、ルーティング、命名規則、デザインを尊重し、関係のない機能やデータは変更しないでください。

このアプリに、次の機能をすべて実装してください。

## 1. 避難所登録の情報確認日時

- 「情報確認日時」は `datetime-local` など、日時を入力できるコントロールにする。
- 入力欄の近くに「現在時刻」ボタンを置く。
- ボタンを押すと、利用者のブラウザのローカル時刻を入力欄へ設定する。
- HTML の `datetime-local` が要求する `YYYY-MM-DDTHH:mm` 形式にする。
- 登録時は既存の保存形式と互換性を保ち、入力された値を保存する。

## 2. 施設区分の追加

既存の施設区分を維持したまま、少なくとも次の選択肢を追加する。

- 防災倉庫
- 物資集積所
- その他

登録処理は施設区分を固定値で限定しすぎず、選択された値を既存のデータ形式に従って保存する。

## 3. 共通ヘッダー右上の操作

全画面で利用できる共通ヘッダーの右上に、次の操作を追加する。

- 言語切り替えボタン
- 文字サイズ・配色設定ボタン
- ログアウトボタン

既存のログイン状態を利用し、ログイン中のみログアウト操作を表示する。ログアウト処理は既存の `/logout` などの処理を再利用し、認証方式を作り直さない。

## 4. 言語切り替え

言語切り替えボタンを押したら、見た目だけでなく画面表示も日本語と英語で切り替わるようにする。

- `<html lang>` を切り替える。
- 共通ヘッダー、ナビゲーション、ログイン、検索、登録、発信ボードなどの固定表示文言を英語化する。
- 入力欄の placeholder、aria-label、ボタン名、選択肢名も対象にする。
- 施設名、住所、利用者が入力した発信内容、気象 API から取得した動的データなどの利用者データは壊さず、そのまま表示する。
- 日本語へ戻す操作も可能にする。
- 言語設定は `localStorage` などで保存し、画面遷移後も維持する。
- 翻訳辞書または既存の i18n 機構を使い、画面ごとに不整合な処理を増やさない。

## 5. 文字サイズ・配色設定

文字・配色ボタンは、押すたびに順番に切り替える方式ではなく、クリック時に選択メニューを表示する方式にする。

選択肢は次の4つとする。

- 標準
- 大きめ文字
- 高コントラスト
- 大きめ文字＋高コントラスト

要件:

- 現在選択中の項目をメニュー上で分かるようにする。
- 選択後はメニューを閉じる。
- メニュー外をクリックした場合も閉じる。
- 設定は `localStorage` などに保存し、画面遷移後も維持する。
- 大きめ文字ではレイアウト崩れを起こさない。
- 高コントラストでは文字、背景、境界線、ボタンの視認性を確保する。
- `aria-expanded`、`aria-controls` などを設定し、キーボード操作やスクリーンリーダーでも利用できるようにする。

## 6. 重複登録の防止

避難所登録時、保存処理の前に既存データと照合する。

- 既存の施設名と一致する場合は登録しない。
- 入力住所が空欄でない場合、既存の住所と一致する場合も登録しない。
- 比較時は前後の空白を除去する。
- 重複時は利用者に分かる日本語のエラーを表示する。
- エラー時には入力済みのフォーム値を保持する。
- 重複データを保存ファイルへ追加しない。
- 既存データに施設名・住所が存在しない場合でも例外を起こさない。

エラー判定はクライアント側だけに頼らず、必ずサーバー側の登録処理で行う。

## 7. 災害対応区分の複数選択

既存の単一選択を複数選択へ変更する。

- 津波、地震、洪水をチェックボックスで表示する。
- 複数の区分を同時に選択できるようにする。
- サーバー側では `getlist` 相当の方法で全選択値を取得する。
- 保存形式は配列または対象システムで標準的な複数値形式にする。
- 1つも選択されていない場合は登録を拒否し、エラーを表示する。
- 入力エラー時は選択状態を保持する。
- 既存データが単一文字列形式の場合は、表示や読み込みでエラーにならないよう互換性を考慮する。

## 8. 避難所登録画面の完全仕様とデザイン

避難所登録画面は、単に入力項目を並べるだけでなく、次の項目、配置、見た目、レスポンシブ動作まで再現する。

### 入力項目

登録フォームには次の項目を用意する。`name` 属性と保存項目の対応も維持する。

| 項目 | name | 入力形式 | 必須 | 備考 |
| --- | --- | --- | --- | --- |
| 施設区分 | `facility_type` | select | いいえ | 指定避難所、一時避難所、福祉避難所、防災倉庫、物資集積所、その他 |
| 施設名 | `name` | text | はい | 重複登録を禁止 |
| 施設名（ローマ字） | `name_roman` | text | はい | 英語表記の入力欄 |
| 現在の受入れ人数 | `accepted_count` | number | いいえ | 0以上 |
| 情報確認日時 | `check_time` | datetime-local | いいえ | 「現在時刻」ボタン付き |
| 避難可能人数（収容） | `capacity` | number | はい | 0以上 |
| 備蓄食料人数 | `stock_count` | number | いいえ | 0以上 |
| 施設住所 | `address` | textarea | はい | 重複登録を禁止。地図表示にも使用 |
| 施設住所（英語表記） | `address_english` | text | いいえ | 英語表記の入力欄 |
| 災害対応区分 | `disaster_type` | checkbox 複数 | はい | 津波、地震、洪水 |

フォーム下部には、利用者が内容を確認したことを示す `confirm` チェックボックスと、登録実行用の「登録する」ボタンを配置する。`confirm` は必須とする。

### レイアウトと見た目

- 共通ヘッダーの下に、中央寄せの登録ページを配置する。
- 最大幅はおよそ 1120px、左右に余白を設ける。
- フォーム全体は白色または半透明白色のカード内に配置する。
- カードは薄い境界線、控えめな影、角丸を使用する。
- 見出しは「避難所情報登録」とし、避難所を表す小さな建物アイコンを左側に置く。
- デスクトップでは3列のフォームレイアウトにし、各列を上から下へ整列する。
- 画面幅が狭い場合は2列、さらに狭いスマートフォンでは1列へ切り替える。
- スマートフォンではカードの余白と見出しを小さくし、登録ボタンを横幅いっぱいにする。
- ラベルは入力欄の上に置き、必須項目には赤色の「必須」を表示する。
- 入力欄、select、textarea は同じ高さ・境界線・角丸・文字サイズで統一する。
- 入力欄のフォーカス時は青色の境界線と薄いアウトラインを表示する。
- textarea は複数行入力でき、最低高さを確保する。
- 災害対応区分と確認チェックボックスは、チェックボックスとラベルを横並びにする。
- 内容確認エリアは薄い青色の背景で囲み、登録前の確認箇所として目立たせる。
- 登録ボタンは青色の塗りつぶし、白文字、軽い影を使い、ホバー時に濃い青へ変化させる。
- 成功メッセージは緑系、エラーメッセージは赤系の背景と文字色で表示する。
- エラーや成功メッセージには `role="alert"` を付ける。
- 既存の共通ヘッダーの色、CSS 変数、フォント、余白の設計と調和させる。

### 登録画面の状態

- 初回表示では空の入力欄を表示する。
- バリデーションエラー、重複エラー、保存エラーでは入力済みの全項目とチェック状態を復元する。
- 登録成功時は成功メッセージを表示し、次の登録ができるようフォームを初期化する。
- 現在時刻ボタンは通常の送信を起こさず、日時欄だけを更新する。
- 既存のログイン必須制御を維持し、未ログイン利用者が登録画面を直接操作できないようにする。

## 実装上の注意

- 既存の未変更機能、認証、ルーティング、データを壊さない。
- 関係のないリファクタリングや依存パッケージの追加は避ける。
- 既存のデザインに合わせ、共通ヘッダーに操作をまとめる。
- ボタンやメニューには適切な `type`、`aria-label`、キーボード操作を設定する。
- 外部ライブラリを追加する前に、既存の HTML、CSS、JavaScript、Jinja 機能で実現できるか確認する。
- 既存の保存処理を使い、重複チェックを通過した場合だけ保存する。
- 保存失敗時にメモリ上の追加データだけが残らないよう、既存のエラー処理を維持する。

## 完了条件

実装後、次を確認してください。

1. Python の構文チェックとテンプレートエラーチェックが通る。
2. JavaScript の構文チェックが通る。
3. 避難所登録画面で現在時刻ボタンが動作する。
4. 施設区分に防災倉庫、物資集積所、その他が表示される。
5. 既存の施設名または住所で登録するとエラーになり、データが増えない。
6. 災害対応区分を2つ以上選択して登録すると、すべて保存される。
7. 災害対応区分を未選択で送信するとエラーになる。
8. ヘッダー右上から日本語と英語を切り替えられる。
9. 文字サイズ・配色ボタンを押すと4つの選択肢が表示され、選んだ設定が維持される。
10. ログイン中に右上のログアウトボタンを押すと既存のログアウト処理が実行される。

変更したファイル、実行した検証コマンド、未対応の既存仕様があれば最後に簡潔に報告してください。
